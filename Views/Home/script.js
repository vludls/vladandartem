/*class Elem {
	constructor(selector) {
		this.elem = document.querySelectorAll(selector);
	}

	html (value) {
		alert(this.elem.length);
	}

	
}

var elem = new Elem('p');
elem.html('!');
*/



















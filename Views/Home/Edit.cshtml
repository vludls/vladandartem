@model vladandartem.Models.Product
@{
    ViewData["Title"] = "Redact Product";
}

        <main class="container" id="redact">
            <img src="@Model?.ImgPath" alt="" id="imgProd">
            @if(ViewBag.Errors != null)
            {
                <ul>
                    @foreach (var error in ViewBag.Errors.ErrorMessages)
                    {
                        <li>@error</li>
                    }
                </ul>
            }
            <form action="~/Home/Edit" id="add" method="post" enctype="multipart/form-data">
                <p>Изображение:</p>
                <input type="hidden" name="id" value="@Model?.Id">
                <input type="hidden" name="imgpath" value="@Model?.ImgPath">
                <input type="file" name="fileimg" id="file">
                <p>Название:</p>
                <input type="text" name="name" value="@Model?.Name">
                <p>Цена:</p>
                <input type="text" name="price" value="@Model?.Price">
                <p>Производитель:</p>
                <input type="text" name="manufacturer" value="@Model?.Manufacturer">
                <p>Категория:</p>
                <input type="text" name="category" value="@Model?.Category">
                <input type="submit" value="Изменить" id="addProduct">

                <!--<h2>Операционная система</h2>
                <p>Операционная система:</p>
                <input type="text">
                <p>Обновление операционной системы:</p>
                <input type="text">

                <h2>Процессор</h2>
                <p>Производитель процессора</p>
                <input type="text">
                <p>Тип процессора</p>
                <input type="text">
                <p>Количество ядер процессора</p>
                <input type="text">
                <p>Макс. такт. частота</p>
                <input type="text">
                <p>Кэш-память</p>
                <input type="text">

                <h2>Оперативная память</h2>
                <p>Оперативная память (RAM)</p>
                <input type="text">
                <p>Тип памяти</p>
                <input type="text">
                <p>Частота памяти</p>
                <input type="text">-->
                
            </form>
        </main>

        